<UserControl x:Class="Projet_tut_ACCA.Vue.UCCotisation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projet_tut_ACCA.Vue"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid ItemsSource="{Binding Path=persons, UpdateSourceTrigger=PropertyChanged}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Adherent.Nom, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Adherent.Statut, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Paiement" Binding="{Binding Adherent.Statut, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Adherent.Adresse, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Adherent.Tel, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
